## 相关资料在本机：E:\Download\STM\奋斗店铺_STM32H743VIT_OPENMV

# 基于单目视觉的目标物测量装置 — 设计文档

> **目标平台**: 奋斗STM32H743VIT开发板 + OV5640摄像头  
> **开发环境**: MDK-ARM (Keil) 5.3, STM32 HAL库  
> **CPU**: STM32H743VIT6 (Cortex-M7, 480MHz主频, 2MB Flash, 1MB SRAM)  
> **本文档用途**: 作为AI IDE的prompt，指导代码生成和模块实现  
> **范围**: 仅包含视觉测量部分（不含电流检测电路）

---

## 一、系统总体架构

```
[OV5640摄像头] → [DCMI+DMA] → [图像缓冲区0x24000000] → [视觉处理流水线] → [结果显示在LCD]
                                     ↑                                      ↓
                              [按键(K1/K2)触发]                       [SD卡记录(可选)]
```

系统采用单帧触发模式：按键触发后，DMA捕捉一帧Y8灰度图像到内存，随后CPU执行图像处理与测量算法，结果在LCD上显示。全流程目标耗时 < 5秒。

---

## 二、硬件资源分配

| 硬件外设 | 功能 | 关键配置 |
|----------|------|----------|
| DCMI | 摄像头像素数据采集 | 8位数据模式，硬件同步，PCLK上升沿，VS/HS低电平有效，全帧捕获 |
| DMA2 Stream7 | 像素数据搬运 | 外设→内存，32位字宽传输，单帧快照模式(DMA_NORMAL) |
| OV5640 | 500万像素摄像头传感器 | SCCB接口配置，传感器窗口 **1280×960** (4:3比例)，输出 **Y8灰度** 格式（每像素1字节） |
| SPI1 | 1.69寸 TFT LCD 显示 | 240×280分辨率，64K色 |
| USART1 | 调试串口 | 115200bps, 8N1 |
| GPIOE3 | 蓝色状态LED | 推挽输出 |
| GPIOE12 | 摄像头补光LED控制 | 推挽输出 |
| GPIOD10 | OV5640 PWDN控制 | 掉电/唤醒 |
| GPIOE13 | OV5640 RST控制 | 硬件复位 |
| SDMMC1 | Micro SD卡 | SDIO 4位模式，FATFS文件系统（保存调试截图用） |
| 按键(K1/K2) | 触发测量 | PE4/PE5，外部中断模式 |

### 内存布局

```
AXI SRAM (0x24000000 - 0x24080000, 512KB):
  0x24000000: Camera_Buffer (640×480×1 = 307,200字节，约300KB — 单帧Y8灰度)
  0x2404B000: Binary_Buffer (640×480×1 = 307,200字节，约300KB — 二值化结果)
  → 两帧合计600KB，需分别放在不同的512KB块中（AXI SRAM共512KB，不足同时存放）
  → 推荐方案: Camera_Buffer放AXI SRAM (0x24000000)，二值化结果放入 SRAM1 (0x30000000)

SRAM1 (0x30000000 - 0x30020000, 128KB):
  - 二值图缓冲区 (80×60降采样1字节，或全分辨率需借助平铺处理)
  - 轮廓点数组
  - 中间计算结果

SRAM2 (0x30020000 - 0x30048000, 160KB):
  - 备选处理空间

DTCM (0x20000000 - 0x2001C000, 112KB — 低延迟、无cache):
  - 关键算法栈变量、查找表、直方图
```

**内存策略总结**:
- OV5640输出Y8格式，传感器窗口1280×960经DCMI硬件裁剪至 **640×480**，每像素1字节，帧大小 **300KB**
- 300KB < 512KB (AXI SRAM)，单帧完整放入
- 二值图可按8倍降采样存储（用1 bit表示1像素，全帧仅需 **38.4KB**），或采用分块流式处理（STMF即扫即处理，无需全帧存储）
- 原先RGB565的600KB超出单片RAM限制，改用Y8灰度从根本上解决了内存瓶颈

---

## 三、模块划分与实现方案

### 模块1: 系统初始化与调度 (system_init)

**文件**: `Core/Src/main.c`, `Core/Inc/main.h`

**任务**:
- 配置MPU：0x24000000起512KB设为Full Access + 可缓存
- 使能I-Cache和D-Cache
- 系统时钟：HSE(24MHz) → PLL → 400MHz CPU / 200MHz AHB / 100MHz APB
- 外设初始化：LED、按键、USART1、LCD(SPI1)、OV5640(DCMI+DMA)、SD卡(可选)
- 主循环：等待按键触发 → 触发测量流程 → 显示结果 → 回到等待

**实现要点**:
- 主循环空闲时进入WFI低功耗模式减少功耗
- 按键采用外部中断(EXTI)，中断服务函数中置标志位，主循环轮询检测
- 测量流程必须单帧触发：`OV5640_DMA_Transmit_Snapshot()`，捕获一帧后自动停止DCMI
- 帧捕获完成后 `OV5640_FrameState` 标志置1，在while(1)中检测该标志启动处理流水线

**关键代码结构**:
```c
// 初始化流程
MPU_Config(); SCB_EnableICache(); SCB_EnableDCache();
HAL_Init(); SystemClock_Config();
LED_Init(); KEY_Init(); USART1_Init();
SPI_LCD_Init(); DCMI_OV5640_Init();
// 可选: MX_SDMMC1_SD_Init(); MX_FATFS_Init();

// 主循环
while(1) {
    if(measure_trigger) {
        measure_trigger = 0;
        OV5640_DMA_Transmit_Snapshot(Camera_Buffer, BufferSize);
        // 等待 OV5640_FrameState == 1
        while(!OV5640_FrameState) { __WFI(); }
        OV5640_FrameState = 0;
        // 执行测量流水线
        process_and_measure();
        // 显示结果
        display_results();
    }
    __WFI();
}
```

---

### 模块2: 摄像头配置 (camera_config)

**文件**: `Drivers/User/Src/dcmi_ov5640.c`, `Drivers/User/Inc/dcmi_ov5640.h`

**任务**: 配置OV5640输出适合视觉处理的图像

**关键配置**:
- 输出格式: **Y8灰度**（OV5640配置为YUV422输出，DCMI配置为仅提取Y分量，或直接配置传感器寄存器输出RAW8 Grayscale）
- 传感器窗口: **1280×960** (4:3原生比例，充分利用传感器视场)
- DCMI硬件裁剪窗口: **640×480**，裁剪区域对准摄像头视野下半部分（目标物区域）
- DCMI配置: 8位数据，硬件同步，全帧捕获，不使用JPEG模式
- 帧捕获模式: **DMA_NORMAL**（单帧快照，捕获一帧后自动停止）
- **帧大小**: 640×480×1字节 = **300KB**，完美装入AXI SRAM (512KB)

**实现要点**:
- `OV5640_DCMI_Crop(640, 480, 1280, 960)` 进行硬件级裁剪，减小内存占用
- OV5640寄存器配置为YUV422输出，DCMI侧配置为只取Y分量（每2字节中取第1字节），等效Y8灰度输出
- 若OV5640支持RAW8 Grayscale模式则直接配置，跳过YUV→灰度转换
- 关闭自动白平衡的过度响应：固定白平衡参数以适应室内光照
- 设置适中的对比度和亮度：提高黑色图形与白色背景的区分度
- 补光LED在捕获前自动开启，捕获完成后关闭
- 图像水平镜像和垂直翻转按实际安装方向调整

**参考**: 已有`dcmi_ov5640.c`提供了完整的SCCB配置、裁剪、AF自动对焦等函数，直接复用。需要调整的是：
- `OV5640_Width` 和 `OV5640_Height` 改为1280和960
- `Display_Width` 和 `Display_Height` 改为640和480
- 在OV5640配置表中加入固定白平衡参数

---

### 模块3: 图像预处理 (image_preprocess)

**文件**: `Core/Src/image_proc.c`, `Core/Inc/image_proc.h`

**任务**: 对DCMI捕获的Y8灰度帧进行降噪、二值化，输出二值图

**子步骤**:

#### 3.1 高斯滤波（降噪）
- 直接对`Camera_Buffer`中的Y8灰度数据进行滤波，无需颜色空间转换
- 使用 **3×3** 大小的高斯核（在精度和速度间取平衡）
- 核权重: `[1,2,1; 2,4,2; 1,2,1] / 16`
- 使用可分离卷积优化：先水平方向3×1，再垂直方向1×3，计算量从9次/像素降至6次/像素
- 边界处理：镜像填充或跳过最外圈1像素

#### 3.2 图像二值化（双策略，自适应切换）

**主策略 — Otsu大津算法（全局阈值）**:
- Otsu原理：最大化类间方差 σ² = w0·w1·(μ0-μ1)²
- 实现：先计算灰度直方图(256 bins)，遍历0-255找到使类间方差最大的阈值T
- 二值化：大于T为白色(背景)，小于T为黑色(目标)
- 对Y8数据（0=黑到255=白），在场景可控（白纸黑图形）时表现良好

**备用策略 — 积分图加速的自适应均值阈值（局部阈值）**:
当赛场存在顶灯阴影、局部反光或纸面倾斜造成的亮度渐变时，Otsu全局阈值可能导致部分区域过曝或全黑。此时自动切换为局部自适应阈值：

```c
// 使用积分图加速的局部均值阈值法
void adaptiveThreshold(uint8_t *src, uint8_t *dst, 
                       int width, int height, int blockSize, int C)
{
    // Step 1: 计算积分图 integral_img
    // integral_img[x,y] = sum of src[0..x, 0..y]
    uint32_t *integral = malloc((width+1)*(height+1)*sizeof(uint32_t));
    for(int y=0; y<height; y++)
        for(int x=0; x<width; x++)
            integral[(y+1)*(width+1)+(x+1)] = src[y*width+x]
                + integral[(y+1)*(width+1)+x]
                + integral[y*(width+1)+(x+1)]
                - integral[y*(width+1)+x];
    
    // Step 2: 使用积分图快速计算每个像素的 blockSize×blockSize 邻域均值
    // blockSize 建议取 31~51 奇数，C 取 5~10
    int half = blockSize / 2;
    for(int y=0; y<height; y++) {
        for(int x=0; x<width; x++) {
            int x1 = MAX(0, x-half), x2 = MIN(width-1, x+half);
            int y1 = MAX(0, y-half), y2 = MIN(height-1, y+half);
            int count = (x2-x1+1) * (y2-y1+1);
            uint32_t sum = integral[(y2+1)*(width+1)+(x2+1)]
                         - integral[(y2+1)*(width+1)+x1]
                         - integral[y1*(width+1)+(x2+1)]
                         + integral[y1*(width+1)+x1];
            uint8_t mean = (uint8_t)(sum / count);
            dst[y*width+x] = (src[y*width+x] < (mean - C)) ? 0 : 255;
        }
    }
    free(integral);
}
```

**决策逻辑**:
> 默认使用Otsu二值化。若检测到异常（黑色像素占比<10%或>90%，或后续A4纸角点提取失败），则自动切换至自适应阈值模式（blockSize=41, C=8）重试。重试仍失败则触发补光灯强制开启后重新捕获。

**二值图压缩存储**:
- 采用 **1 bit/像素** 的C-STRIP位压缩格式存储二值图，640×480 → **38.4KB**
- 或者直接在主循环中执行"滤波→二值化→边缘检测"的流式处理，省去显存中的全帧二值图

---

### 模块4: A4纸边界检测与ROI提取 (paper_detector)

**文件**: `Core/Src/paper_detect.c`, `Core/Inc/paper_detect.h`

**任务**: 在图像中找到A4纸的黑色边框，提取纸张区域作为后续测量的ROI

**算法流程**:

#### 4.1 轮廓检测
1. 对二值图进行 **Canny边缘检测**：
   - Sobel算子计算梯度幅值和方向
   - 非极大值抑制
   - 双阈值滞后边缘连接（低阈值50，高阈值150）

2. 连通域分析：对边缘图做8邻域连通域标记

3. 对每个连通域做Douglas-Peucker多边形逼近

#### 4.2 A4纸四边形定位
- 在所有轮廓中查找**面积最大的四边形**
- 该四边形应满足：凸、面积占图像比例合理（A4纸在100-200cm处约占图像面积的15%-40%）
- 对四边形顶点排序：[左上, 右上, 右下, 左下]
- A4纸的实际宽高比是210mm:297mm ≈ 1:1.414，可作为验证条件

**简化方案（推荐用于嵌入式）**:
由于场景高度可预测（A4纸白色，边框黑色2cm宽，始终竖立在轴线上），使用更高效的方案：
1. 从图像下半部分开始逐行扫描，找到黑色→白色→黑色过渡（即找到水平边框线）
2. 利用Hough直线检测寻找A4纸的水平和垂直边框线
3. 根据A4纸比例验证四条线的几何关系

#### 4.3 图像裁剪
- 将四边形区域通过透视变换校正为矩形
- 提取的ROI分辨率归一化为固定尺寸（如400×566，保持A4纸比例）
- 同时在原图中记录A4纸的实际像素宽度 `W_paper_px`（用于距离计算）

---

### 模块5: 几何图形检测 (shape_detector)

**文件**: `Core/Src/shape_detect.c`, `Core/Inc/shape_detect.h`

**任务**: 在A4纸ROI内检测黑色实心几何图形，确定其类型和像素尺寸

**算法流程**:

#### 5.1 图形区域定位
- 在ROI内再次做轮廓检测
- 过滤条件：面积在合理范围（A4纸的10%-30%面积对应10-16cm图形），内部实心黑色
- 排除A4纸边框线（距离ROI边缘的轮廓可忽略）

#### 5.2 形状判别

对每个候选轮廓：

**方法A — Hu矩匹配（推荐）**:
1. 计算轮廓的Hu不变矩（7个Hu矩）
2. 与预计算的圆、等边三角形、正方形的Hu矩模板进行匹配
3. 使用最小距离分类器确定形状类型

**方法B — 几何特征判别（更快，精度更依赖轮廓质量）**:
1. 计算轮廓面积 `A` 和周长 `P`
2. 计算圆形度: `C = 4πA/P²` → 圆接近1.0，正方形≈0.785，三角形≈0.605
3. 计算最小外接矩形(MBR)的宽高比 → 正方形约1:1，等边三角形约0.866:1
4. 综合判定:
   - C > 0.85 且 MBR宽高比≈1 → **圆形**
   - 0.7 < C < 0.85 且 MBR宽高比≈1 → **正方形**
   - C < 0.7 且顶点数≈3或MBR宽高比≈0.866 → **等边三角形**

#### 5.3 像素尺寸测量
- **圆形**: 像素直径 `d_px` = 最小外接圆的直径，或等效直径 `d_px = 2*sqrt(A/π)`
- **等边三角形**: 像素边长 `l_px` = `sqrt(4*A/√3)`（等边三角形面积公式反推）
- **正方形**: 像素边长 `l_px` = `sqrt(A)` 或取四条边的平均像素长度

---

### 模块6: 单目视觉距离与尺寸解算 (monocular_solver)

**文件**: `Core/Src/mono_solver.c`, `Core/Inc/mono_solver.h`

**任务**: 利用已知尺寸的A4纸作为标定参照物，通过直接比例关系同时解算目标距离D和真实尺寸x

#### 6.1 直接比例公式（省去焦距标定）

采用**单应比例模型**，利用A4纸作为天然标定物。A4纸(210mm×297mm)在图像中的像素尺寸与目标图形的像素尺寸之间存在固定比例关系：

```
x_real(mm)  =  x_px * A4_WIDTH_MM  /  W_paper_px
D(mm)       =  D_known * W_paper_known / W_paper_px   (可由标定表查得)
```

更简洁的**核心公式**——将距离和尺寸统一为一条比例链：

```
x_real = x_px * A4_WIDTH_MM / W_paper_px
```

其中：
- `x_real`: 目标图形真实尺寸(mm)
- `x_px`: 目标图形在图像中的像素尺寸（由模块5提供）
- `A4_WIDTH_MM`: 210.0mm（若测量竖直方向则用297.0mm）
- `W_paper_px`: A4纸在图像中的像素宽度（由模块4提供）

**优点**:
1. 无需单独标定相机焦距f：A4纸本身即标定物，比例直接抵消未知的焦距系数
2. 仅需一次整数乘法与一次整数除法，可在定点运算中完成，避免浮点误差累积
3. 对摄像头安装位置的微小偏差具有自补偿能力（只要每次测量时A4纸和目标处于同一平面）

#### 6.2 距离D的查表或反推

距离D不直接参与x的计算，但仍需输出显示。使用以下方法之一计算D：

**方法A — 标定查表法（推荐，最稳定）**:
- 赛前标定：在已知距离点（100, 120, 140, 160, 180, 200cm）采集A4纸像素宽度，建立查找表
```c
// 查找表 (距离cm → A4纸像素宽度)
const uint16_t dist_table[][2] = {
    {1000, 0},  // 将由标定过程填充实际像素宽度
    {1200, 0},
    {1400, 0},
    {1600, 0},
    {1800, 0},
    {2000, 0},
};
```
- 运行时根据实测`W_paper_px`在表中线性插值得到D

**方法B — 直接比例反推**:
```
D = f_eff / W_paper_px
```
其中`f_eff = D_ref * W_ref_px`（在某一参考距离处标定一次获得）。此方法等价于经典针孔模型，但f_eff直接用像素·mm单位存储，避免浮点。

#### 6.3 精度提升策略

- **双轴验证**: 同时利用A4纸宽度(210mm)和高度(297mm)分别计算x，两者应一致，取平均减少单轴检测噪声
- **多帧平均**: 对同一目标连续采集3帧，取x的3次均值（前提：总耗时仍<5秒）
- **亚像素边缘**: 在测量A4纸边框和图形边缘时，对Sobel梯度峰值做抛物线插值获取亚像素精度

> **误差分析**: 对D=1000-2000mm范围，A4纸宽度(210mm)在640×480图像中的像素宽度约为80-160px。若边缘定位误差±2px，x的测量误差约为±(1.2%-2.5%），在10-16cm的图形上对应误差约1.2-4.0mm，满足基本要求(≤1cm)和发挥要求(≤0.5cm)。

---

### 模块7: 倾斜目标物校正（发挥部分第4项） (tilt_correction)

**文件**: `Core/Src/tilt_correct.c`, `Core/Inc/tilt_correct.h`

**任务**: 当目标物倾斜30°-60°时，校正透视畸变以准确测量正方形边长

#### 7.1 倾斜角检测
- 利用A4纸边框在图像中的四边形形状判断倾斜
- 正常情况下垂直线应为平行线 → 若边框的左右两边呈汇聚趋势，说明物面倾斜
- 通过四边形顶点的几何关系计算倾斜角θ

#### 7.2 仿射校正
1. 利用A4纸的四个角点和已知的A4纸长宽比，估计单应矩阵H (3×3)
2. 使用**透视逆变换**将倾斜物面纠正为正视图
3. 透视变换公式: 对于原图中点(u,v)，校正后坐标(x,y):
   ```
   [x' y' w] = H⁻¹ × [u v 1]ᵀ
   x = x'/w, y = y'/w
   ```
4. 在校正后的图像上重新执行模块5和模块6

#### 7.3 嵌入式优化
- 不校正整个图像，而是将正方形的四个角点坐标进行透视变换，直接在校正空间中计算边长
- 这样可以大幅减少计算量，避免对整个A4纸区域做像素级重映射

---

### 模块8: 多正方形目标处理（发挥部分） (multi_square)

**文件**: `Core/Src/multi_square.c`, `Core/Inc/multi_square.h`

**任务**: 处理包含多个正方形的组合图形或带编号的正方形

#### 8.1 多正方形分割
- 在ROI内检测所有黑色正方形轮廓
- 对于部分重叠的正方形：使用分水岭算法或基于凸缺陷的分割
- 对每个分割出的正方形分别计算像素边长

#### 8.2 最小面积正方形判定
- 对所有检测到的正方形，比较像素面积
- 面积最小者即为目标

#### 8.3 编号识别（带数字的正方形）

在每个正方形区域内识别白色阿拉伯数字编号（目标物描述：在黑色正方形内印有1位白色数字）。

**算法策略 — 基于拓扑几何特征的轻量级识别**:

由于数字是印刷体且为黑白二值，使用基于连通域拓扑结构的识别方法，对轻微旋转和透视形变鲁棒，且无需浮点运算适合MCU。

**特征提取**（对二值化后的正方形内部区域）:

| 特征 | 计算方法 | 区分能力 |
|------|----------|----------|
| 空洞数(holeCount) | 对黑色背景中的白色数字区域做连通域分析，统计数字内部完全被白色包围的黑色区域个数 | 0个洞：1,2,3,5,7；1个洞：0,6,9；2个洞：8 |
| 宽高比(aspectRatio) | 数字区域外接矩形的宽度/高度 | 1明显更窄(<0.6)；7略窄(0.7-0.85)；0/6/8/9接近1 |
| 水平中线穿线次数(crossCount) | 沿数字高度50%处扫描一行，统计白→黑→白的跳变次数 | 区分4(2次穿线)、7(1次)、大部分数字(3次以上) |
| 上部闭合性(hasTopLoop) | 检查数字顶部1/3区域是否存在闭合环 | 9有上部环、6没有上部环 |
| 重心偏上(centerBiasUp) | 数字区域重心Y坐标是否明显偏上 | 区分7(重心偏上) |

**决策树（按优先级依次判定）**:

```
if (holeCount == 2) → '8'
else if (holeCount == 1) {
    if (hasTopLoop) → '9'
    else {
        if (aspectRatio > 0.85) → '0'
        else → '6'  // 6略扁
    }
}
else { // holeCount == 0
    if (aspectRatio < 0.55) → '1'
    else if (crossCount == 2) → '4'
    else if (crossCount == 1) → '7'
    else {
        // 剩余: 2,3,5 → 用垂直对称性和上半部凸包特征区分
        // 2: 上部弧形（凸包顶部为弧线）
        // 3: 上下对称，有两处水平线段
        // 5: 上部为平直线
        if (hasStraightTop) → '5'
        else if (hasUpperCurve) → '2'
        else → '3'
    }
}
```

**实现要点**:
- 所有特征均基于像素遍历和连通域标记，无需乘除运算，完全定点实现
- 白色数字区域的提取：在正方形ROI内对二值图做反色（白底黑字→黑底白字），再提取连通域
- 对旋转≤20°、透视畸变≤15%均保持鲁棒
- ROI在进入此模块前已经过模块7的透视校正，数字基本处于正视状态，进一步提高识别率

---

### 模块9: LCD显示 (display)

**文件**: 复用板载 `lcd_169_drv.c`, 封装在 `Core/Src/display.c`

**任务**: 在1.69寸(240×280) LCD上显示测量结果

**显示内容**:
```
========测量结果========
目标类型:   [圆/三角形/正方形]
距离D:      xxx.x cm
图形尺寸x:  xx.x cm
=======================
```

**实现要点**:
- 复用已有的LCD驱动函数：`LCD_DisplayString()`, `LCD_DisplayNumber()`
- 在捕获图像后可以显示缩略图预览（降采样到240×180区域）
- 使用`LCD_CopyBuffer()`的裁剪和缩放功能

---

## 四、关键算法参考

### 4.1 Otsu大津算法伪码

```
function OtsuThreshold(gray_image):
    计算灰度直方图hist[256]
    总像素数 N = width * height
    
    best_threshold = 0
    max_variance = 0
    
    for t in 0..255:
        w0 = sum(hist[0..t]) / N          // 背景像素占比
        w1 = 1 - w0                        // 前景像素占比
        if w0==0 or w1==0: continue
        
        mu0 = sum(i*hist[i] for i in 0..t) / (w0*N)  // 背景均值
        mu1 = sum(i*hist[i] for i in t+1..255) / (w1*N) // 前景均值
        
        variance = w0 * w1 * (mu0 - mu1)²
        if variance > max_variance:
            max_variance = variance
            best_threshold = t
            
    return best_threshold
```

### 4.2 Canny边缘检测简化版

```
function CannyEdges(gray_image):
    // Step 1: Sobel梯度
    for each pixel (x,y):
        Gx = SobelX_3x3(gray, x, y)
        Gy = SobelY_3x3(gray, x, y)
        magnitude[x][y] = sqrt(Gx² + Gy²)  // 或近似: |Gx|+|Gy|
        direction[x][y] = atan2(Gy, Gx)
    
    // Step 2: 非极大值抑制
    for each pixel:
        q = 255, r = 255
        // 根据梯度方向选择邻域像素比较
        angle = direction[x][y] 量化到 [0°,45°,90°,135°]
        比较 magnitude 与同方向的邻域像素
        如果不是局部最大值: magnitude[x][y] = 0
    
    // Step 3: 双阈值 + 滞后
    high = 150, low = 50
    for each pixel:
        if magnitude > high: edge[x][y] = STRONG
        elif magnitude > low: edge[x][y] = WEAK
    // 连接: 弱边缘只有与强边缘相邻时才保留
```

### 4.3 透视变换简化（4点→4点）

给定源四边形顶点 `src[4]`（A4纸四角像素坐标），目标矩形顶点 `dst[4]` = [(0,0), (w,0), (w,h), (0,h)]，其中 `w = A4_WIDTH_MM * scale`, `h = A4_HEIGHT_MM * scale`。

使用OpenCV风格的`getPerspectiveTransform`简化实现：
- 构造8×8线性方程组
- 使用高斯消元法求解单应矩阵H（3×3）的8个未知数（H[2][2]固定为1）

---

## 五、代码文件结构

```
Project/
├── Core/
│   ├── Inc/
│   │   ├── main.h              // 主头文件，全局宏定义
│   │   ├── config.h            // 标定数据、阈值等配置
│   │   ├── image_proc.h        // 图像预处理
│   │   ├── paper_detect.h      // A4纸检测
│   │   ├── shape_detect.h      // 图形检测与分类
│   │   ├── mono_solver.h       // 单目测距解算
│   │   ├── tilt_correct.h      // 倾斜校正
│   │   ├── multi_square.h      // 多正方形处理
│   │   └── display.h           // LCD显示封装
│   └── Src/
│       ├── main.c              // 主程序，调度逻辑
│       ├── image_proc.c        // Y8灰度→高斯滤波、Otsu/自适应二值化
│       ├── paper_detect.c      // Canny、轮廓、四边形定位、透视校正
│       ├── shape_detect.c      // Hu矩/几何特征分类、像素尺寸测量
│       ├── mono_solver.c       // 直接比例公式解算距离D、尺寸x
│       ├── tilt_correct.c      // 倾角检测、仿射校正
│       ├── multi_square.c      // 多正方形分割与编号识别
│       ├── display.c           // LCD结果显示
│       ├── stm32h7xx_it.c      // 中断服务（EXTI按键、DCMI帧完成等）
│       └── stm32h7xx_hal_msp.c // HAL外设MSP初始化
├── Drivers/
│   └── User/                   // 板载驱动（已提供，直接复用）
│       ├── dcmi_ov5640.c/h     // OV5640驱动
│       ├── lcd_169_drv.c/h     // LCD驱动
│       ├── sccb.c/h            // SCCB协议
│       └── usart.c/h           // 串口
└── MDK-ARM/                    // Keil工程文件
```

---

## 六、配置参数 (`config.h`)

```c
// ===== 相机参数（仅用于辅助计算，核心公式无需f） =====
#define IMG_WIDTH               640
#define IMG_HEIGHT              480
#define CAMERA_BUFFER_ADDR      0x24000000UL    // AXI SRAM, 512KB
#define BINARY_BUFFER_ADDR      0x30000000UL    // SRAM1, 二值图(位压缩38.4KB)

// ===== A4纸参考尺寸（标定参照物） =====
#define A4_WIDTH_MM             210.0f
#define A4_HEIGHT_MM            297.0f
#define A4_BORDER_WIDTH_MM      20.0f    // 黑色边框线宽2cm

// ===== 距离D标定查找表（赛前标定填入） =====
// 格式: {距离mm, 对应的A4纸像素宽度}
// 运行时根据实测W_paper_px在线性插值得到D
typedef struct {
    uint16_t dist_mm;
    uint16_t width_px;
} DistCalEntry;

const DistCalEntry dist_cal_table[] = {
    {1000, 0},  // 标定时填入实测像素宽度
    {1200, 0},
    {1400, 0},
    {1600, 0},
    {1800, 0},
    {2000, 0},
};
#define DIST_CAL_TABLE_SIZE     6

// ===== 测量范围 =====
#define DIST_MIN_MM             1000.0f  // 最小测量距离 100cm
#define DIST_MAX_MM             2000.0f  // 最大测量距离 200cm
#define TARGET_SIZE_MIN_MM      60.0f    // 最小目标尺寸 6cm
#define TARGET_SIZE_MAX_MM      160.0f   // 最大目标尺寸 16cm

// ===== 图像处理阈值 =====
#define CANNY_LOW_THRESHOLD     50
#define CANNY_HIGH_THRESHOLD    150
#define GAUSS_KERNEL_SIZE       3       // 高斯核大小 (3×3)
#define MIN_CONTOUR_AREA        500     // 最小轮廓面积(像素)

// ===== 二值化策略参数 =====
#define OTSU_BLACK_RATIO_MIN    0.10f   // Otsu判定有效的最小黑色像素占比
#define OTSU_BLACK_RATIO_MAX    0.90f   // Otsu判定有效的最大黑色像素占比
#define ADAPTIVE_BLOCK_SIZE     41      // 自适应阈值邻域大小（奇数）
#define ADAPTIVE_CONSTANT_C     8       // 自适应阈值常数偏移量
```

---

## 七、实现优先级与分层策略

### 第一层（必须实现 — 基础要求）
1. 系统初始化 + 摄像头配置 + LCD显示 (模块1, 2, 9)
2. 图像预处理：高斯滤波 + Otsu/自适应二值化（输入已是Y8灰度，无需颜色转换）(模块3)
3. A4纸四边形检测 + 像素宽度提取 (模块4 — 简化版)
4. 单目距离D解算 (模块6)
5. 单一形状检测与像素尺寸测量 (模块5)
6. 真实尺寸x解算 + 结果显示

### 第二层（发挥部分）
7. 多正方形分割 (模块8)
8. 数字编号识别 (模块8)
9. 倾斜校正 (模块7)

### 第三层（优化增强）
10. 处理速度优化（充分利用STM32H743的DSP指令、FPU）
11. 测量精度优化（多次平均、滑动窗口滤波）
12. 低功耗优化（WFI、外设时钟门控）

---

## 八、调试与验证建议

1. **A4纸标定验证**: 在已知距离处拍摄A4纸，打印检测到的像素宽度填入`dist_cal_table`，验证距离D插值精度
2. **分模块调试**: 每个图像处理模块完成后，将中间结果通过串口导出到PC端查看（降采样后发送）
3. **SD卡保存帧**: 出错时将原始帧保存为BMP文件到SD卡供离线分析
4. **性能测量**: 使用SysTick计时每个模块的执行时间，确保总耗时<5秒
5. **边缘case**: 测试不同光照、不同距离的鲁棒性
